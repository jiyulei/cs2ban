export type Teammate = string;

export type Teammates = {
  gameId: string;
  teammates: Teammate[];
  date: string;
};

export type Enemy = string;

export type Enemies = {
  gameId: string;
  enemies: Enemy[];
  date: string;
};